# Core dependencies
semgrep>=1.0.0        # SAST scanning, supports Python, JS/TS, Java, Go, .NET, more
gitleaks>=8.18.0      # Secrets scanning
jinja2>=3.0.0         # HTML report generation
openai>=1.0.0         # AI-powered remediation (use GPT-3.5-turbo for cost)
python-dotenv>=0.19.0 # .env support for secrets
requests>=2.26.0
typing-extensions>=4.0.0
pathlib>=1.0.1
gitpython>=3.1.0      # For repo interaction
# Syft/Trivy are recommended as external binaries for dependency/SCA scanning
